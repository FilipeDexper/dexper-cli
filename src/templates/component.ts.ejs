import { Flex } from '@chakra-ui/react';

const <%= props.name%> = () => {
  //const styles = useMultiStyleConfig('<%= props.name%>') as <%= props.name%>BaseStyle;
  //const styles = useStyleConfig('<%= props.name%>') as <%= props.name%>BaseStyle;

  return <Flex data-testid="<%= props.name%>"></Flex>;
};

<%= props.name%>.displayName = '<%= props.name%>';

export default <%= props.name%>;
